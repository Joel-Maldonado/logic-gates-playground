# Fonts

Bundled fonts used by the UI profile:

- `SpaceGrotesk-Medium.ttf` (primary UI font)
- `JetBrainsMono-Regular.ttf` (mono/status font)

Optional fallback fonts:

- `InterTight-Regular.ttf`
- `IBMPlexSans-Regular.ttf`

License notices are in `assets/fonts/LICENSES.md`.
