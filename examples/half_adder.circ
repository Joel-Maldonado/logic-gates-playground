# Half adder:
#   SUM   = A XOR B
#   CARRY = A AND B

input A
input B
xor XOR1
and AND1
output SUM
output CARRY

wire A -> XOR1.in0
wire B -> XOR1.in1
wire A -> AND1.in0
wire B -> AND1.in1
wire XOR1.out0 -> SUM.in0
wire AND1.out0 -> CARRY.in0
